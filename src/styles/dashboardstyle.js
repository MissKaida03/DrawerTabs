import { StyleSheet } from 'react-native';

const white = '#fff'; 

const styles = StyleSheet.create({
  dashboardContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f1d5b8',
  },
  dashboardTitle: {
    fontSize: 30,
    color: white,
  },
});

export default styles;